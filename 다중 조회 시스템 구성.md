다중 조회 시스템 구성
define max 1001*10 = 11000
1. 인자로 받아온 시즌의 범위만큼 반복 예시(1~10)

반복(1~10){
	우선 앞이랑 똑같이 데이터를 받아옴
	반복(1001){
	만약 닉네임이 중복되는 구조체가 있는가{
		구조체를 생성
		구조체에 해당 유저의 데이터 삽입
		}
	이미 존재하는가{
		구조체 배열을 돌면서 중복되는 구조체 찾기
		해당 구조체를 찾으면 각종 데이터를 합연산해서 갱신
	}
	}

