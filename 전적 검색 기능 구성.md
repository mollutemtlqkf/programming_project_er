전적 검색 기능 구성

1. 유저 닉넴 받기
-popen 파이프 생성
2. URL 한국어 인코딩을 한후 앞에 있는 템플릿에 갖다박아서 명령어 엑조디아 완성하기
-URL 인코딩 방법
0.한국어인지 아닌지 확인 (영어인가 숫자인가 아니면 한국어야? 밑으로 ㄱ)
1. 한글자마다 3바이트씩 쪼갠다
2.각 바이트별로 4칸 오른쪽 시프트 / &연산자로 가리기?

3. 명령어를 실행

4. cjson을 이용해서 uid 확보 

5. uid 받은것을 이용해서 전적 검색 하기
- 여기서 한번더 함수로 빠지기 (프린트까지 다하는것)

ppt 코멘트 : 원래 userNum이 전광판 api 호출할 때 같이 불러와져서 구조체에 userNum을 저장한뒤 그걸 기반으로 해당 유저를 자세히 들여다 보는 기능을 구현할려 했으나
보안 패치로 인해 userNum을 받아오지 않고 UID만 받아오도록 api 구조가 바뀜
따라서 전광판에서 userNum이 원래 딸려오던것도 사라져서 원래 구조체에 userNum을 저장한걸 기반으로 url 인코딩 없이 전적을 가져올려 했으나, api 구조 변경 때문에 어쩔수 없이 전체 전적을 가져오는것으로 기능을 바꿈







